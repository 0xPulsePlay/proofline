# Base Sepolia (live delivery / deploy) — env-only, never commit real values
BASE_RPC_URL=https://sepolia.base.org
DEPLOYER_PRIVATE_KEY=
FORWARDER_PRIVATE_KEY=

# TxLINE free tier (live ingestion)
TXLINE_API_TOKEN=
# or a file containing the token / an access.json with { "apiToken": ... }
TXLINE_API_TOKEN_FILE=

# Control Room live mode (optional; replay works without it)
NEXT_PUBLIC_COORDINATOR_URL=
COORDINATOR_URL=
